{
  "name": "pocket-speechify",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "build:wasm": "bash scripts/build-wasm.sh",
    "test": "vitest run",
    "test:watch": "vitest",
    "package": "wxt zip"
  },
  "dependencies": {
    "wxt": "^0.20.20"
  },
  "devDependencies": {
    "happy-dom": "^20.8.4",
    "vitest": "^4.1.0"
  }
}
